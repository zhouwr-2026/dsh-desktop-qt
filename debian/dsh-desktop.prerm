#!/bin/sh
set -e
if [ -d /run/systemd/system ]; then
    deb-systemd-invoke stop dsh-theme-export.path >/dev/null || true
    deb-systemd-invoke disable dsh-theme-export.path >/dev/null || true
fi
#DEBHELPER#
